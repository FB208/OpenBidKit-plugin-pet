# 易标投标工具箱 - 插件开发指南

## 概述

易标投标工具箱插件系统允许开发者通过独立的 npm 包扩展软件功能。插件以预编译包形式发布，通过 GitHub Release 分发，用户可以在客户端直接下载安装。

**核心特性：**
- 插件以独立 GitHub 仓库开发，通过 Release 发布
- 插件运行时使用主程序注入的依赖，不重复占用内存
- 支持 Main 进程和配置界面开发
- 完整的权限控制和隔离机制

---

## 快速开始

### 1. 创建插件项目

```bash
mkdir yibiao-plugin-demo
cd yibiao-plugin-demo
npm init -y
```

### 2. 项目结构

```
yibiao-plugin-demo/
├── package.json
├── manifest.json          # 插件元信息（必需）
├── main.cjs              # Main 进程入口（可选）
├── config-ui/            # 配置界面（可选）
│   ├── index.html
│   ├── style.css
│   └── script.js
├── assets/
│   └── icon.png          # 插件图标 128x128px（必需）
└── README.md
```

### 3. 编写 manifest.json

```json
{
  "id": "demo-plugin",
  "name": "示例插件",
  "version": "1.0.0",
  "description": "这是一个示例插件",
  "author": "你的名字",
  "repository": "https://github.com/yourusername/yibiao-plugin-demo",
  "releaseUrl": "https://github.com/yourusername/yibiao-plugin-demo/releases/download/v1.0.0/yibiao-plugin-demo-v1.0.0.zip",
  "permissions": [
    "task:read",
    "task:subscribe"
  ],
  "hasConfig": false
}
```

### 4. 编写 main.cjs

```javascript
module.exports = {
  async activate(ctx) {
    ctx.logger.info('插件已激活');
    
    // 订阅任务事件
    const unsubscribe = ctx.onTaskEvent((event) => {
      ctx.logger.info('收到任务事件:', event);
    });
    
    // 保存 unsubscribe 函数供 deactivate 使用
    this.unsubscribe = unsubscribe;
  },
  
  async deactivate() {
    if (this.unsubscribe) {
      this.unsubscribe();
    }
    ctx.logger.info('插件已停用');
  }
};
```

### 5. 准备图标

在 `assets/` 目录下放置 `icon.png`，尺寸 128x128px。

### 6. 打包发布

```bash
# 打包为 zip
zip -r yibiao-plugin-demo-v1.0.0.zip . -x "*.git*" "node_modules/*"

# 在 GitHub 创建 Release 并上传 zip 包
```

---

## manifest.json 字段说明

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `id` | string | 是 | 插件唯一标识，全局唯一，建议使用小写字母和连字符 |
| `name` | string | 是 | 插件显示名称 |
| `version` | string | 是 | 版本号，遵循语义化版本 (semver) |
| `description` | string | 是 | 插件描述，在插件市场展示 |
| `author` | string | 否 | 作者名称 |
| `repository` | string | 是 | GitHub 仓库地址，用于自动拼接图标 URL |
| `releaseUrl` | string | 是 | GitHub Release 下载地址 |
| `permissions` | string[] | 是 | 权限列表，见下文 |
| `hasConfig` | boolean | 是 | 是否有配置界面 |
| `configUI` | string | 否 | 配置界面入口 HTML 文件路径（相对于插件根目录） |

---

## 权限系统

插件必须在 `manifest.json` 中声明所需权限，主程序根据权限决定暴露哪些 API。

| 权限 | 说明 | 提供的 API |
|------|------|-----------|
| `task:read` | 读取任务状态 | `getActiveTasks()`, `getTechnicalPlanState()`, `getDuplicateCheckState()`, `getRejectionCheckState()` |
| `task:subscribe` | 订阅任务事件 | `onTaskEvent()` |
| `agent:question` | 响应 Agent 问答 | `getPendingAgentQuestion()`, `onAgentQuestion()`, `answerAgentQuestion()`, `suppressAgentQuestionAutoAnswer()` |
| `task:confirm` | 确认一级目录选择 | `confirmOutlineSelection()`, `suppressOutlineSelectionAutoConfirmation()` |
| `agent:workspace` | 与 Agent 工作空间对话 | `listAgentWorkspaces()`, `sendAgentWorkspaceMessage()`, `onAgentWorkspaceChatEvent()`, `onAgentWorkspacesChanged()` |
| `window:create` | 创建独立窗口 | `createWindow()` |

---

## Main 进程 API

### ctx.logger

日志输出，所有日志会带上插件 ID 前缀。

```javascript
ctx.logger.info('信息日志');
ctx.logger.warn('警告日志');
ctx.logger.error('错误日志');
```

### ctx.store

插件专用配置存储，数据保存在 `userData/plugins/<plugin-id>/config.json`。

```javascript
// 读取配置
const value = ctx.store.get('key');

// 写入配置
ctx.store.set('key', 'value');
```

### ctx.getActiveTasks()

**权限：** `task:read`

获取当前运行中的任务列表。

```javascript
const tasks = ctx.getActiveTasks();
console.log(tasks);
```

### ctx.onTaskEvent(callback)

**权限：** `task:subscribe`

订阅所有任务事件，实时获取进度更新。

**返回值：** 返回一个 `unsubscribe` 函数，调用它可以取消订阅。

```javascript
const unsubscribe = ctx.onTaskEvent((event) => {
  console.log('任务事件:', event);
  
  // event 结构：
  // {
  //   task: { ... },
  //   technicalPlan: { ... },
  //   rejectionCheck: { ... },
  //   duplicateCheck: { ... }
  // }
});

// 取消订阅
unsubscribe();
```

正文生成任务的 `task.progress` 表示整个正文任务的累计进度。需要展示当前细阶段时读取 `task.progress_detail`，其中 `phase_label`、`phase_progress` 和 `step_label` 分别表示阶段名称、阶段进度和当前子步骤说明。

### ctx.getTechnicalPlanState()

**权限：** `task:read`

获取技术方案工作区状态。

```javascript
const state = ctx.getTechnicalPlanState();
```

### ctx.getDuplicateCheckState()

**权限：** `task:read`

获取查重工作区状态。

```javascript
const state = ctx.getDuplicateCheckState();
```

### ctx.getRejectionCheckState()

**权限：** `task:read`

获取废标检查工作区状态。

```javascript
const state = ctx.getRejectionCheckState();
```

### Agent 问答 API

插件可复用主程序当前的 Agent 问题，在独立窗口中增加快捷回答入口：

```javascript
const unsubscribe = ctx.onAgentQuestion((question) => {
  if (!question) return;
  console.log(question.question, question.options);
});

const pending = ctx.getPendingAgentQuestion();
ctx.answerAgentQuestion({
  question_id: pending.question_id,
  option_id: pending.options[0].id,
});
```

用户主动选择选项时，可先调用 `ctx.suppressAgentQuestionAutoAnswer({ question_id })` 停止当前问题的自动回答倒计时。任一入口提交答案后，主程序会向所有订阅者推送 `null`，插件应关闭自己的问答界面。

### 一级目录选择 API

**权限：** `task:confirm`

一级目录待确认状态通过任务事件观察：`outline-generation` 任务的 `task.stats.outline_selection` 含 `items`、`selected_ids`、`auto_answer_at`、`confirmed`。插件可代替主界面提交确认：

```javascript
ctx.confirmOutlineSelection({
  taskId: task.task_id,
  items: selection.items,
  selectedIds: selection.selected_ids,
});
```

用户主动操作时可先调用 `ctx.suppressOutlineSelectionAutoConfirmation({ taskId })` 停止自动确认倒计时。任一入口确认后，主程序会推送 `outline_selection.confirmed: true` 的任务事件，插件应关闭自己的选择界面。

### Agent 工作空间 API

**权限：** `agent:workspace`

主程序向插件暴露可对话的通用 Agent 工作空间（当前内置目录生成与全局事实设定）。主程序按当前可见页标记唯一生效项；`active` 表示当前可见页的生效工作空间，不是库里上次的步骤。插件只打开 `active === true` 的工作空间，不要回退到 `workspaces[0]`：

```javascript
// 列出可对话的工作空间；无可用工作空间时返回空数组
const workspaces = ctx.listAgentWorkspaces();
// [{ id, title, status, busy_reason, has_generated_content, empty_hint, send_warning?, active, pending, messages }]
const active = workspaces.find((item) => item.active) || null;

// 发送用户要求；必须发给当前生效项，否则拒绝。消息入列后立即返回，最终回复通过聊天事件推送
ctx.sendAgentWorkspaceMessage({ workspaceId: active.id, message: '把安全管理章节拆成两章' });

// 订阅聊天状态（用户消息入列、pending、Agent 最终回复、错误）
const unsubscribeChat = ctx.onAgentWorkspaceChatEvent((event) => {
  // event = { workspace_id, messages, pending }
});

// 订阅工作空间列表与生效项变更（切步、空间就绪变化）
const unsubscribeWorkspaces = ctx.onAgentWorkspacesChanged((event) => {
  // event = { active_workspace_id, workspaces }
  // 打开中且 id 不等于 active 时关闭对话框，不要自动打开新空间
});
```

打开规则：只打开生效项；没有则关闭对话并提示「当前步骤没有可对话的工作空间」。切步骤等于离开旧工作空间：关掉对话框，不自动打开新的。空对话提示用 `empty_hint`；发送前警告用 `send_warning`（没有该字段则不弹）。`has_generated_content` 表示已有正文叶子，由主程序据此决定是否下发 `send_warning`。

### 宿主事件（onHostEvent）

主程序可通过 `pluginService.notifyPluginEvent(pluginId, event, payload)`（Renderer 侧 `window.yibiao.plugins.notifyEvent()`）向已启用插件发送宿主事件。插件通过可选导出 `onHostEvent(event, payload)` 接收，例如主程序「AI调整」按钮发送的 `open-ai-chat`。该事件只负责打开当前生效工作空间，忽略 payload。

### ctx.createWindow(options)

**权限：** `window:create`

创建独立窗口（例如桌宠窗口）。

**参数：** `options` - Electron `BrowserWindowConstructorOptions`

**返回值：** `BrowserWindow` 实例

```javascript
const win = ctx.createWindow({
  width: 400,
  height: 300,
  frame: false,
  transparent: true,
  alwaysOnTop: true,
});

win.loadFile('path/to/your/html');
```

### ctx.app

Electron `app` 实例（只读），可用于获取路径、版本等信息。

```javascript
const userDataPath = ctx.app.getPath('userData');
const appVersion = ctx.app.getVersion();
```

### ctx.ipcMain

Electron `ipcMain` 实例，插件可以注册自己的 IPC 通道。

```javascript
ctx.ipcMain.handle('plugin:demo:action', async (event, arg) => {
  return { success: true, data: arg };
});
```

---

## 配置界面开发

### 1. 启用配置界面

在 `manifest.json` 中设置：

```json
{
  "hasConfig": true,
  "configUI": "./config-ui/index.html"
}
```

### 2. 创建配置界面

`config-ui/index.html`:

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>插件配置</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <h1>插件配置</h1>
  
  <label>
    设置项 1:
    <input type="text" id="setting1" />
  </label>
  
  <button id="saveBtn">保存</button>
  
  <script src="script.js"></script>
</body>
</html>
```

`config-ui/script.js`:

```javascript
// 主程序会注入 window.pluginConfig API

// 读取配置
async function loadConfig() {
  const value = await window.pluginConfig.get('setting1');
  document.getElementById('setting1').value = value || '';
}

// 保存配置
document.getElementById('saveBtn').addEventListener('click', async () => {
  const value = document.getElementById('setting1').value;
  await window.pluginConfig.set('setting1', value);
  alert('配置已保存');
});

// 页面加载时读取配置
loadConfig();
```

### 3. 配置窗口 API

主程序会通过 preload 注入 `window.pluginConfig`：

```typescript
window.pluginConfig = {
  get: (key: string) => Promise<any>,
  set: (key: string, value: any) => Promise<boolean>
};
```

---

## 生命周期钩子

插件必须暴露 `activate` 和 `deactivate` 方法；需要实时响应配置页保存时，可选暴露 `onConfigChange`；需要接收主程序宿主事件时，可选暴露 `onHostEvent`：

```javascript
module.exports = {
  /**
   * 插件启用时调用
   * @param {object} ctx - 插件上下文
   */
  async activate(ctx) {
    // 初始化插件
    // 订阅事件
    // 创建窗口
  },

  /**
   * 配置页保存后调用（可选）
   * @param {{ key: string, value: any }} change - 本次配置变化
   */
  async onConfigChange(change) {
    // 将配置即时应用到运行中的插件
  },

  /**
   * 主程序宿主事件（可选），例如 'open-ai-chat'
   * @param {string} event - 事件名
   * @param {any} [payload] - 事件负载
   */
  async onHostEvent(event, payload) {
    // 响应主程序发起的联动动作
  },
  
  /**
   * 插件停用时调用
   */
  async deactivate() {
    // 清理资源
    // 取消订阅
    // 关闭窗口
  }
};
```

---

## 示例：桌面宠物插件

完整示例见：https://github.com/xxx/yibiao-plugin-desktop-pet

**功能：**
- 在桌面显示透明窗口
- 实时监听任务进度
- 任务完成后弹出提醒

**核心代码：**

```javascript
let petWindow = null;
let unsubscribe = null;

module.exports = {
  async activate(ctx) {
    ctx.logger.info('[desktop-pet] 插件已激活');
    
    // 创建桌宠窗口
    petWindow = ctx.createWindow({
      width: 200,
      height: 200,
      frame: false,
      transparent: true,
      alwaysOnTop: true,
      webPreferences: {
        preload: path.join(__dirname, 'preload.js'),
      },
    });
    
    petWindow.loadFile(path.join(__dirname, 'pet.html'));
    
    // 订阅任务事件
    unsubscribe = ctx.onTaskEvent((event) => {
      // 将进度发送到桌宠窗口
      if (petWindow && !petWindow.isDestroyed()) {
        petWindow.webContents.send('task-progress', event);
      }
    });
  },
  
  async deactivate() {
    if (unsubscribe) {
      unsubscribe();
    }
    if (petWindow && !petWindow.isDestroyed()) {
      petWindow.close();
    }
  }
};
```

---

## 打包与发布

### 1. 打包插件

```bash
# 方式 1：使用 zip 命令
zip -r yibiao-plugin-demo-v1.0.0.zip . -x "*.git*" "node_modules/*" ".DS_Store"

# 方式 2：使用 npm scripts
# 在 package.json 中添加：
# "scripts": {
#   "build": "zip -r yibiao-plugin-demo-$npm_package_version.zip . -x '*.git*' 'node_modules/*'"
# }
npm run build
```

### 2. 创建 GitHub Release

1. 在 GitHub 仓库中创建 Release
2. Tag 版本号：`v1.0.0`
3. 上传打包好的 zip 文件
4. 复制 zip 下载链接，更新 `manifest.json` 中的 `releaseUrl`

### 3. 提交到插件市场

联系易标团队，提供：
- 插件 GitHub 仓库地址
- Release 下载地址
- 插件描述和图标

---

## 调试技巧

### 1. 查看插件日志

插件的 `ctx.logger` 输出会显示在主程序的控制台中（开发模式）。

### 2. 本地测试

将插件解压到 `userData/plugins/<plugin-id>/`，然后在插件管理页面启用即可测试。

### 3. 错误处理

插件的 `activate` 和 `deactivate` 方法应该使用 try-catch：

```javascript
async activate(ctx) {
  try {
    // 插件初始化代码
  } catch (error) {
    ctx.logger.error('激活失败:', error);
    throw error;
  }
}
```

---

## 最佳实践

1. **保持插件轻量**：只包含必要的代码，避免大依赖
2. **错误处理**：所有异步操作都应该有错误处理
3. **资源清理**：在 `deactivate` 中清理所有资源
4. **权限最小化**：只申请必要的权限
5. **版本兼容**：在 README 中说明支持的主程序版本范围
6. **文档完善**：提供清晰的安装和使用说明

---

## 常见问题

### Q: 插件可以访问文件系统吗？

A: 可以，但只能通过主程序注入的 `ctx.app` 获取路径。不要硬编码路径。

### Q: 插件可以使用 npm 包吗？

A: 可以，但需要在打包前安装依赖，并将 `node_modules` 包含在 zip 中。建议使用轻量依赖。

### Q: 如何更新插件？

A: 发布新的 GitHub Release，更新 `manifest.json` 中的 `version` 和 `releaseUrl`，用户在客户端点击"更新"即可。

### Q: 插件可以修改主程序的 UI 吗？

A: 当前版本不支持。插件只能创建独立窗口。

---

## 相关链接

- [插件 API 参考文档](./插件API参考.md)
- [示例插件仓库](https://github.com/xxx/yibiao-plugin-examples)
- [问题反馈](https://github.com/FB208/OpenBidKit_Yibiao/issues)
